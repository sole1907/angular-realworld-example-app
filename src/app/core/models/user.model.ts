export interface User {
  email: string;
  lastName: string;
  firstName: string;
  otherNames: string;
  token: string;
  username: string;
  bio: string;
  image: string;
  following: boolean;
  active: boolean;
  verified: boolean;
  role: string;
}
